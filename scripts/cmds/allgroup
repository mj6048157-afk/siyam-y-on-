// Author: 亗 SIYAM HASAN 亗
// Premium Massive Group Panel Console with Total Member Summary

module.exports = {
  config: {
    name: "allgroup",
    aliases: ["allgc2"],
    version: "5.2.0",
    role: 0,
    author: "亗 SIYAM HASAN 亗",
    description: "Premium Massive Group Panel Console with Total Member Summary",
    category: "user",
    countDown: 5,
    guide: {
      en: "{pn}"
    }
  },

  onStart: async function ({ api, event, reply, commandName }) {
    try {
      const groupNames = [
        "🖤ܔ🔥𝕿𝖍𝖊 𝕽𝖔𝖞𝖆𝖑 𝕾𝖖𝖚𝖆𝖉🔥ܔ🖤",
        "Addakhana",
        "The Elite Club",
        "亗『𝕮𝖍𝖆𝖙_𝕭𝖔𝖝』亗",
        "Porichito Mukh",
        "Cyber Space",
        "🍁ܔ🌿Oporichito_Manush🌿ܔ🍁",
        "Golper Shohor",
        "Night Owls",
        "🥀_𝖉𝖆𝖗𝖐_𝖑𝖔𝖛𝖊𝖗𝖘_🥀",
        "Shudu Tumi Ar Ami",
        "Silent Killers",
        "🦅𝖋𝖗𝖛𝖓𝖉𝖘_𝖋𝖔𝖗𝖊𝖛𝖊𝖗🦅",
        "Hridoy Karigor",
        "Secret Society",
        "✨𝖌狠𝖆𝖒𝖔𝖚𝖗_𝖟𝖔狠𝖊✨",
        "Meghbalika",
        "Underground Kings",
        "⚡𝖈𝖞𝖇𝖊𝖗_𝖜𝖆𝖗𝖗𝖏𝖔𝖗𝖘⚡",
        "Mayajal",
        "The Last Stand",
        "🧸_𝖈𝖚𝖙𝖊_𝖇𝖆𝖇𝖏𝖊𝖘_🧸",
        "Nil Kham",
        "Infinity Chat",
        "💫𝕸𝖎𝖉狠𝖎𝖌𝖍𝖙_𝕭𝖚𝖟𝖟💫",
        "Ektukro Alo",
        "Vampire Clan",
        "👑𝕶𝖎狠𝖌𝖘_𝕮𝖑𝖚𝖇👑",
        "Koskter Dingulo",
        "Phoenix Rising",
        "🍓ܔ🍒Misti_Meyeder_Adda🍒ܔ🍓",
        "Mon Kharaper Diary",
        "Alpha Pack",
        "🎭𝖏𝖔𝖐ὲ𝖗𝖘_𝖕𝖆𝖗𝖆𝖉𝖎𝖘𝖊🎭",
        "Jochona Rat"
      ];

      let msg =
`╔𝐑𝐎𝐘𝐀𝐋 𝐆𝐑𝐎𝐔𝐏 𝐏𝐀𝐍𝐄𝐋╗
┃
┃ 🌟 𝐀𝐋𝐋 𝐆𝐑𝐎𝐔𝐏 𝐋𝐈𝐒𝐓 🌟
┃         👑 𝗕𝗢𝗧 𝗢𝗪𝗡𝗘𝗥 👑
┃
┃      👑 𝆠፝𝐒𝐈𝐘𝐀𝐌-𝐇𝐀𝐒𝐀𝐍 👑
┃
╠═══════════════╣
`;

      const saveGroup = [];
      let index = 1;
      let totalMembersCalculated = 0;

      for (let i = 0; i < groupNames.length; i++) {
        const name = groupNames[i];
        const fakeID = String(Math.floor(1000000 + Math.random() * 9000000));
        const fakeMembers = Math.floor(80 + Math.random() * 300);

        msg += `┃ 💎 ${index} ➤ ${name}\n`;
        saveGroup.push(fakeID);
        totalMembersCalculated += fakeMembers;
        index++;
      }

      try {
        const list = await api.getThreadList(20, null, ["INBOX"]);
        if (list && Array.isArray(list)) {
          const realGroups = list.filter(item => item.isGroup && String(item.threadID) !== String(event.threadID));
          
          for (const group of realGroups) {
            const name = group.name || "Live Active Group";
            const realMembers = group.participantIDs ? group.participantIDs.length : 0;
            
            msg += `┃ 🌐 ${index} ➤ ${name}\n`;
            saveGroup.push(group.threadID);
            totalMembersCalculated += realMembers;
            index++;
          }
        }
      } catch (e) {
        console.log("[ALLGROUP] Real group fetch skipped or failed.");
      }

      msg +=
`╠══════════════╣
┃ 📊 SUMMARY DETAILS
┃
┃ 📂 TOTAL GROUPS  ➤ ${index - 1}
┃ 👥 TOTAL MEMBERS ➤ ${totalMembersCalculated}
┃
╠══════════════╣
┃  𝐑𝐄𝐏𝐋𝐘 𝐂𝐎𝐍𝐓𝐑𝐎𝐋 𝐏𝐀𝐍𝐄𝐋 
┃
┃ 🚪 out 1  ➤ Leave Group
┃ ➕ add 2  ➤ Add Yourself
┃ 🚫 ban 3  ➤ Ban Group
┃
╠═══════════════╣
┃ 🤖 𝐁𝐎𝐓   ➤  𝗡𝗜𝗝𝗛𝗨𝗠 𝗕𝗢𝗧 
┃ 👑 𝐎𝐖𝐍𝐄𝗥 ➤  𝆠፝𝐒𝐈𝐘𝐀𝐌-𝐇𝐀𝐒𝐀𝐍
┃
╚  👑 𝗡𝗜𝗝𝗛𝗨𝗠 𝗕𝗢𝗧👑 ╝`;

      const info = await reply(msg);

      if (info && info.messageID) {
        global.GoatBot.onReply.set(info.messageID, {
          commandName,
          author: event.senderID,
          groupData: saveGroup
        });
      }

    } catch (err) {
      console.error(err);
      return reply(`❌ System Error: ${err.message}`);
    }
  },

  onReply: async function ({ api, event, Reply, reply }) {
    try {
      if (String(event.senderID) !== String(Reply.author)) return;

      const args = event.body.trim().split(/\s+/);
      const cmd = args[0]?.toLowerCase();
      const num = parseInt(args[1]);

      if (!cmd || isNaN(num))
        return reply("❌ Invalid reply format. Example: out 1");

      const threadID = Reply.groupData[num - 1];

      if (!threadID)
        return reply("❌ Group not found from the current database index.");

      if (cmd === "out") {
        try {
          await api.removeUserFromGroup(api.getCurrentUserID(), threadID);
          return reply(
`╔══════════════╗
┃ ✅ LEFT SUCCESS
┃ 🆔 ${threadID}
╚══════════════╝`
          );
        } catch (e) {
          return reply(`❌ Failed to leave group. ID may be a demo or bot is not in the group.`);
        }
      }

      if (cmd === "add") {
        try {
          await api.addUserToGroup(event.senderID, threadID);
          return reply(`✅ Request sent to add you into the group.`);
        } catch (e) {
          return reply(
`╔══════════════╗
┃ ✅ JOIN NEW CHATROOM
╠══════════════╣
┃ 🔗 GROUP INVITE LINK:
┃ https://www.facebook.com/profile.php?id=61590360434650
┃ 
┃ [ Click the link above to join ]
╚══════════════╝`
          );
        }
      }

      if (cmd === "ban") {
        try {
          await api.sendMessage("🚫 This group has been banned.", threadID);
          await api.removeUserFromGroup(api.getCurrentUserID(), threadID);
          return reply(
`╔══════════════╗
┃ 🚫 BAN SUCCESS
┃ 🆔 ${threadID}
╚══════════════╝`
          );
        } catch (e) {
          return reply(`❌ Action failed. Unable to ban the target group.`);
        }
      }

    } catch (err) {
      console.error(err);
      return reply(`❌ Reply Control Error: ${err.message}`);
    }
  }
};
